edition: 1.0.0
name: wq-api
access: default
vars:
  region: cn-hangzhou
  service: wq
  function: api

services:
  wq-api:
    component: fc
    props:
      region: ${vars.region}
      service:
        name: ${vars.service}
        description: 围棋在线对战 API
      function:
        name: ${vars.function}
        description: 围棋在线对战 API
        runtime: nodejs16
        codeUri: ./
        handler: index.handler
        memorySize: 256
        timeout: 30
        environmentVariables:
          OTS_ENDPOINT: https://wuziqi.cn-hangzhou.ots.aliyuncs.com
          OTS_INSTANCE: wuziqi
          OTS_TABLE_NAME: wq_room
          OTS_AK_ID: ${env.OTS_AK_ID}
          OTS_AK_SECRET: ${env.OTS_AK_SECRET}
      triggers:
        - name: httpTrigger
          type: http
          config:
            authType: anonymous
            methods:
              - GET
              - POST
              - OPTIONS
